{
  "name": "hasmbly-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/clean.mjs && npm run validate:content && eleventy",
    "dev": "eleventy --serve",
    "validate:content": "node scripts/validate-blogs.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.6",
    "markdown-it": "14.2.0"
  }
}
